# Core FastAPI dependencies (already in base image, but listed for completeness)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.24.0
python-dotenv>=1.0.0
structlog>=23.1.0

# L12-specific dependencies
redis>=5.0.0  # Redis async client for session management and pub/sub

# Optional dependencies for enhanced features
# ollama>=0.1.0  # Semantic search via embeddings (optional - graceful degradation)
